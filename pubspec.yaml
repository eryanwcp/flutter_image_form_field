name: image_form_field
description: Upload images in Flutter forms
version: 0.0.3
homepage: https://github.com/eryanwcp/flutter_image_form_field
author: Tim Shedor <opensource@timshedor.com>

environment:
  sdk: '>=2.10.0 <3.0.0'

dev_dependencies:
  flutter_test:
    sdk: flutter

dependencies:
  flutter:
    sdk: flutter
  image_picker: ^0.8.0
  multi_image_picker: ^4.8.1
  photo_view: ^0.11.1
#  cached_network_image: ^2.2.0+1
#  path_provider: ^1.6.5
#  path: ^1.6.4
